spring:
  application:
    name: psychologist-notebook-bot
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

bot:
  name: test-bot-name
  token: test-token
